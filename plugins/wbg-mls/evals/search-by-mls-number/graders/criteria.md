---
type: llm
---

PASS if the reply resolves the number into one clean listing record — status, price, beds/baths, address, and the listing agent + office as the contact — and keeps any MLS-confidential fields marked agent-only. FAIL if it dumps raw tool output, invents fields the record doesn't carry, or hedges without resolving the number. Format gate: tidy markdown deliverable — title header, at least one facts table, short sections; raw tool dumps fail.
